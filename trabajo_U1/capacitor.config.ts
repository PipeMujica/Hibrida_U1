import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.trabajou1',
  appName: 'trabajou1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
